# EcoGuard Public Assets Repository
- `images/`: High-resolution GIS satellite tiles, topography maps, and hazard overlays.
- `icons/`: Disaster and sensor telemetry SVG vector icons.
- `fonts/`: Self-hosted web fonts for offline field command centers.
